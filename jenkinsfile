1. Setup pipeline which should automatically clone latest from master branch
		a. Build
		b. sonarscan
		c. check quality
		d. Upload binary to jfrog
		e. download binary from jfrog & deploy to tomcat container
		
		
pipeline {
	agent any
	tools {
		maven 'maven'
	}
	stages {
		stage('clone-repo'){
			steps {
				checkout scm
			}
		}
		stage('Build') {
			steps {
				sh 'mvn clean install'
			}
		}
		stage('sonarqube-scanging'){
			steps {
				withSonarQubeEnv('my-sonarqube-server') {
					sh 'mvn clean sonar:sonar'
				}
			}
		}
		stage('quality-gate-check'){
			steps {
				sh 'sleep 30s'
				waitForQualityGate abortPipeline: true
			}
		}
	}
}		
